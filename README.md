# Aaa_work001

C 语言作业仓库，用于上传和管理课程练习代码。

## 内容

- **ex01/hello.c** — Hello World 程序的改进版

### 修改说明

原始程序仅输出 "Hello, World!"，在此基础上做了以下改动：

1. **增加键盘输入** — 使用 `fgets()` 读取用户输入（避免缓冲区溢出，比 `scanf` 更安全）
2. **回显输入内容** — 将用户输入的内容显示输出
3. **中文改英文** — 提示信息改为英文，避免终端中文乱码
